<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.reggie.module.ai.mapper.AIMessageRecordMapper">

    <select id="selectByConversationId" resultType="com.reggie.module.ai.model.AIMessageRecord">
        SELECT id, conversation_id, user_id, role, content, message_type,
               feedback, dish_ids, tokens_used, is_deleted, create_time, tenant_id
        FROM ai_message
        WHERE conversation_id = #{conversationId}
          AND tenant_id = #{tenantId}
          AND is_deleted = #{isDeleted}
        ORDER BY create_time ASC
    </select>

</mapper>
