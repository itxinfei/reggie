# 瑞吉外卖 - 业务逻辑测试流程

## 测试环境

- **数据库**: reggie_db (MySQL 8.0.41)
- **数据量**: 35张表，602条测试数据
- **测试用户**: 用户ID=1 (张小明), 用户ID=2 (李晓红)

---

## 一、客户下单完整流程

### 1.1 购物车管理

**测试场景**: 用户添加商品到购物车

**API**: `POST /shoppingCart/add`

**请求参数**:
```json
{
  "dishId": 1,           // 菜品ID (红烧肉)
  "setmealId": null,     // 套餐ID (点菜时为空)
  "dishFlavor": "[\"微辣\"]",  // 口味
  "number": 2            // 数量
}
```

**验证点**:
- ✅ 购物车商品添加成功
- ✅ 同一菜品+口味组合应该合并数量
- ✅ 不同口味应该分开添加

**数据库验证**:
```sql
SELECT * FROM shopping_cart WHERE user_id = 1;
```

### 1.2 地址簿选择

**测试场景**: 用户选择收货地址

**API**: `GET /addressBook/list`

**验证点**:
- ✅ 返回用户的所有地址
- ✅ 默认地址标记正确 (`is_default = 1`)
- ✅ 地址包含完整的省市区详情

**测试数据**: user_id=1 有2个地址
- id=1: 北京市东城区王府井大街1号 (默认)
- id=2: 北京市西城区金融街8号 (公司)

### 1.3 提交订单

**测试场景**: 用户提交订单

**API**: `POST /order/submit`

**请求参数**:
```json
{
  "addressBookId": 1,    // 地址ID
  "remark": "少辣",       // 备注
  "payMethod": 1,        // 支付方式: 1=微信, 2=支付宝
  "diningType": "DELIVERY"  // 就餐方式: DELIVERY/DINING/TAKEOUT
}
```

**后端处理流程** (OrderServiceImpl.submit()):

1. **获取当前用户ID** (line 59)
   ```java
   Long userId = BaseContext.getCurrentId();
   ```

2. **查询购物车数据** (line 62-64)
   ```java
   LambdaQueryWrapper<ShoppingCart> wrapper = new LambdaQueryWrapper<>();
   wrapper.eq(ShoppingCart::getUserId, userId);
   List<ShoppingCart> shoppingCarts = shoppingCartService.list(wrapper);
   ```

3. **验证数据完整性** (line 66-84)
   - ✅ 购物车不能为空
   - ✅ 用户信息必须存在
   - ✅ 收货地址必须选择

4. **生成订单号** (line 86)
   ```java
   long orderId = IdWorker.getId();
   ```

5. **计算订单金额** (line 88-102)
   ```java
   AtomicInteger amount = new AtomicInteger(0);
   List<OrderDetail> orderDetails = shoppingCarts.stream().map(item -> {
       OrderDetail orderDetail = new OrderDetail();
       orderDetail.setAmount(item.getAmount());
       amount.addAndGet(item.getAmount().multiply(new BigDecimal(item.getNumber())).intValue());
       return orderDetail;
   }).collect(Collectors.toList());
   ```

6. **设置订单属性** (line 105-118)
   ```java
   orders.setId(orderId);
   orders.setOrderTime(LocalDateTime.now());
   orders.setCheckoutTime(LocalDateTime.now());
   orders.setStatus(Orders.STATUS_ORDERED);  // 2 = 待派送
   orders.setAmount(new BigDecimal(amount.get()));
   orders.setUserId(userId);
   orders.setNumber(String.valueOf(orderId));
   orders.setUserName(user.getName());
   orders.setConsignee(addressBook.getConsignee());
   orders.setPhone(addressBook.getPhone());
   orders.setAddress(/* 完整地址 */);
   ```

7. **保存订单和明细** (line 120-123)
   ```java
   this.save(orders);                  // 插入orders表
   orderDetailService.saveBatch(orderDetails);  // 插入order_detail表
   ```

8. **清空购物车** (line 126)
   ```java
   shoppingCartService.remove(wrapper);
   ```

**关键验证点**:
- ✅ **订单状态**: `STATUS_ORDERED = 2` (待派送)
- ✅ **订单号**: 使用雪花算法生成唯一ID
- ✅ **金额计算**: 单价 × 数量累加
- ✅ **事务控制**: `@Transactional(rollbackFor = Exception.class)` 保证原子性
- ✅ **地址拼接**: 省+市+区+详细地址

---

## 二、后台管理流程

### 2.1 订单查询 (分页)

**API**: `GET /order/page`

**请求参数**:
```
page=1&pageSize=10&number=ORD20260101001&beginTime=2026-07-01&endTime=2026-07-03
```

**后端处理** (OrderServiceImpl.orderPage()):

```java
Page<Orders> pageInfo = new Page<>(page, pageSize);
LambdaQueryWrapper<Orders> queryWrapper = new LambdaQueryWrapper<>();

queryWrapper.like(StringUtils.isNotBlank(number), Orders::getNumber, number);

if (StringUtils.isNotBlank(beginTime)) {
    queryWrapper.ge(Orders::getOrderTime, beginTime);
}
if (StringUtils.isNotBlank(endTime)) {
    queryWrapper.le(Orders::getOrderTime, endTime);
}

queryWrapper.orderByDesc(Orders::getOrderTime);
this.page(pageInfo, queryWrapper);
```

**验证点**:
- ✅ 支持订单号模糊查询
- ✅ 支持时间范围筛选
- ✅ 按订单时间倒序排列
- ✅ 分页参数正确

### 2.2 订单状态更新

**API**: `PUT /order`

**请求参数**:
```json
{
  "id": 1,
  "status": 4   // 4 = 已完成 (STATUS_COMPLETED)
}
```

**状态流转**:
```
1 (待付款) → 2 (待派送) → 3 (已派送) → 4 (已完成)
                                   ↓
                                  5 (已取消)
                                   ↓
                                  6 (已退款)
```

**后端处理** (OrderServiceImpl.updateStatus()):

```java
@Transactional(rollbackFor = Exception.class)
public void updateStatus(Integer status, Long id) {
    Orders orders = this.getById(id);
    if (orders != null) {
        orders.setStatus(status);
        this.updateById(orders);
    }
}
```

**验证点**:
- ✅ 订单状态正确更新
- ✅ 幂等性保证（重复调用不会出错）
- ✅ 事务保证数据一致性

### 2.3 再来一单

**API**: `POST /order/again`

**功能**: 将历史订单的商品重新添加到购物车

**后端处理** (OrderServiceImpl.again()):

```java
public void again(Long orderId) {
    // 1. 查询订单明细
    LambdaQueryWrapper<OrderDetail> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(OrderDetail::getOrderId, orderId);
    List<OrderDetail> details = orderDetailService.list(wrapper);

    // 2. 转换为购物车商品
    Long userId = BaseContext.getCurrentId();
    List<ShoppingCart> cartItems = details.stream().map(d -> {
        ShoppingCart cart = new ShoppingCart();
        cart.setName(d.getName());
        cart.setImage(d.getImage());
        cart.setUserId(userId);
        cart.setDishId(d.getDishId());
        cart.setSetmealId(d.getSetmealId());
        cart.setDishFlavor(d.getDishFlavor());
        cart.setNumber(d.getNumber());
        cart.setAmount(d.getAmount());
        cart.setCreateTime(LocalDateTime.now());
        return cart;
    }).collect(Collectors.toList());

    // 3. 批量插入购物车
    shoppingCartService.saveBatch(cartItems);
}
```

**验证点**:
- ✅ 订单明细完整复制到购物车
- ✅ 商品口味、数量、图片等属性保留
- ✅ 绑定到当前登录用户

---

## 三、关键业务规则验证

### 3.1 订单状态常量定义

**文件**: `Orders.java:22-27`

```java
public static final int STATUS_PENDING_PAY = 1;   // 待付款
public static final int STATUS_ORDERED = 2;       // 待派送
public static final int STATUS_DELIVERING = 3;    // 已派送
public static final int STATUS_COMPLETED = 4;     // 已完成
public static final int STATUS_CANCELLED = 5;     // 已取消
public static final int STATUS_REFUNDED = 6;      // 已退款
```

### 3.2 测试数据状态分布

**当前数据库** (15条订单):

| 状态 | 数量 | 说明 |
|------|------|------|
| 1 (待付款) | 2 | 新订单未支付 |
| 2 (待派送) | 0 | 已支付待配送 |
| 3 (已派送) | 0 | 配送中 |
| 4 (已完成) | 12 | 已完成订单 |
| 5 (已取消) | 1 | 用户取消订单 |
| 6 (已退款) | 0 | 退款订单 |

### 3.3 金额计算验证

**代码位置**: OrderServiceImpl.submit() line 88-100

```java
AtomicInteger amount = new AtomicInteger(0);

List<OrderDetail> orderDetails = shoppingCarts.stream().map((item) -> {
    OrderDetail orderDetail = new OrderDetail();
    // ... 其他属性
    orderDetail.setAmount(item.getAmount());
    amount.addAndGet(item.getAmount().multiply(new BigDecimal(item.getNumber())).intValue());
    return orderDetail;
}).collect(Collectors.toList());

orders.setAmount(new BigDecimal(amount.get()));
```

**验证点**:
- ✅ 订单金额 = Σ(菜品单价 × 数量)
- ✅ 支持整数金额（分单位）
- ✅ 购物车不同菜品分别计算

### 3.4 地址拼接逻辑

**代码位置**: OrderServiceImpl.submit() line 115-118

```java
orders.setAddress(
    (addressBook.getProvinceName() == null ? "" : addressBook.getProvinceName())
    + (addressBook.getCityName() == null ? "" : addressBook.getCityName())
    + (addressBook.getDistrictName() == null ? "" : addressBook.getDistrictName())
    + (addressBook.getDetail() == null ? "" : addressBook.getDetail())
);
```

**验证点**:
- ✅ 省市区完整拼接
- ✅ null值处理（防止"null"字符串）
- ✅ 四级地址完整保留

---

## 四、数据验证脚本

### 4.1 验证订单数据

```sql
-- 订单基本查询
SELECT 
    id AS '订单ID',
    number AS '订单号',
    CASE status
        WHEN 1 THEN '待付款'
        WHEN 2 THEN '待派送'
        WHEN 3 THEN '已派送'
        WHEN 4 THEN '已完成'
        WHEN 5 THEN '已取消'
        WHEN 6 THEN '已退款'
    END AS '订单状态',
    amount AS '订单金额',
    user_name AS '用户名',
    dining_type AS '就餐方式'
FROM orders
ORDER BY order_time DESC
LIMIT 10;
```

### 4.2 验证订单明细

```sql
-- 查询订单ID=1的明细
SELECT 
    od.id AS '明细ID',
    od.name AS '商品名称',
    od.number AS '数量',
    od.amount AS '单价',
    od.dish_flavor AS '口味',
    o.number AS '订单号',
    o.status AS '订单状态'
FROM order_detail od
JOIN orders o ON od.order_id = o.id
WHERE od.order_id = 1;
```

### 4.3 验证购物车

```sql
-- 查询用户ID=1的购物车
SELECT 
    sc.id AS '购物车ID',
    sc.name AS '商品名称',
    sc.number AS '数量',
    sc.amount AS '单价',
    sc.dish_flavor AS '口味',
    u.name AS '用户名'
FROM shopping_cart sc
JOIN user u ON sc.user_id = u.id
WHERE sc.user_id = 1;
```

### 4.4 验证状态分布

```sql
-- 订单状态统计
SELECT 
    CASE status
        WHEN 1 THEN '待付款'
        WHEN 2 THEN '待派送'
        WHEN 3 THEN '已派送'
        WHEN 4 THEN '已完成'
        WHEN 5 THEN '已取消'
        WHEN 6 THEN '已退款'
    END AS '订单状态',
    COUNT(*) AS '订单数量',
    SUM(amount) AS '总金额'
FROM orders
GROUP BY status
ORDER BY status;
```

---

## 五、常见业务场景测试用例

### 场景1: 用户首次下单

**前置条件**:
- 用户ID=1，购物车有3个商品
- 地址簿ID=1为默认地址

**测试步骤**:
1. 查询购物车商品
2. 提交订单（不传addressBookId使用默认地址）
3. 验证订单创建成功
4. 验证购物车已清空
5. 验证订单明细正确生成

**预期结果**:
- 订单状态 = 2 (待派送)
- 购物车清空
- 订单明细数量 = 购物车商品数量

### 场景2: 用户取消订单

**测试步骤**:
1. 查询待付款订单 (status=1)
2. 调用 `PUT /order` 更新状态为5
3. 验证订单状态已更新

**预期结果**:
- 订单状态变为5 (已取消)
- 订单金额、用户信息保持不变

### 场景3: 再来一单

**测试步骤**:
1. 查询历史订单 (status=4 或 5)
2. 调用 `POST /order/again`
3. 验证购物车新增历史订单的商品

**预期结果**:
- 购物车新增对应商品
- 商品口味、数量、图片完整保留

### 场景4: 后台订单查询

**测试步骤**:
1. 调用 `GET /order/page?page=1&pageSize=10`
2. 验证返回正确的分页数据
3. 按订单号筛选
4. 按时间范围筛选

**预期结果**:
- 分页数据正确
- 筛选条件生效
- 按时间倒序排列

---

## 六、测试执行步骤

### 方式1: 手动测试（推荐）

1. **启动项目**: 运行 `reggie_take_outApplication.java`
2. **导入数据**: 执行 `src/test/resources/import-data.bat`
3. **使用API工具**: Postman/Insomnia/Apifox
4. **按场景测试**: 参考上面的测试场景

### 方式2: 数据库验证

```bash
# 连接数据库
mysql -u root -p123456 reggie_db

# 执行验证脚本
source src/test/resources/validation-queries.sql
```

### 方式3: 单元测试（开发中）

创建 `OrderServiceTest.java`:
```java
@SpringBootTest
public class OrderServiceTest {
    @Autowired
    private OrderService orderService;
    
    @Test
    public void testSubmitOrder() {
        // 测试下单逻辑
    }
}
```

---

## 七、已知问题

1. ✅ **已修复**: orders表status字段使用字符串而非整数
2. ✅ **已修复**: printer_config表缺少system_printer_name字段
3. ✅ **已修复**: schema.sql缺少外键检查控制语句

---

## 八、测试数据说明

- **用户**: user_id=1 (张小明), phone=13900139001
- **地址**: address_book_id=1 (默认地址)
- **购物车**: 可通过 `/shoppingCart/list` 查询
- **订单**: 15条测试订单，涵盖各种状态

---

**最后更新**: 2026-07-03
**文档版本**: v1.0
